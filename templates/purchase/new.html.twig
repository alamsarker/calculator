{% extends 'base.html.twig' %}

{% block title %}Create New Purchase{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-header">
			Create New Purchase
			<a href="{{ path('purchaseList') }}" class="btn btn-secondary btn-sm" role="button">Back</a>
		</div>
		<div class="card-body">
			{{ include('purchase/_form.html.twig') }}         
		</div>
	</div>    
{% endblock %}
