{% extends 'base.html.twig' %}

{% block title %}Create New Sale{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            Create New Sale
            <a href="{{ path('salesList') }}" class="btn btn-secondary btn-sm" role="button">Back</a>
        </div>
        <div class="card-body">
			      {{ include('form.html.twig') }}
        </div>
    </div>
{% endblock %}
